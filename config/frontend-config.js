// Production runtime configuration
// This file is mounted into the frontend container at runtime
// Update the URLs below for your production environment
window.APP_CONFIG = {
  API_URL: 'http://localhost:8080',
  WS_URL: 'ws://localhost:8080'
};